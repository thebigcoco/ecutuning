# ECU Tuning Table

Static GitHub Pages app for viewing and recalculating PowerFC ECU log tables.

## Usage

Open the GitHub Pages URL, then use the upload controls to load:

- ECU log: `.txt`, `.log`, `.tsv`
- InjMap: `.xlsx`, `.csv`, `.tsv`, `.txt`

Files are parsed in the browser. They are not uploaded to a server.
